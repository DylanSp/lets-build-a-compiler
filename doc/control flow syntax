'e' = end program, endif
'i' = if

IF
<condition>    {  cond = Condition()
                  if (!cond) {
                  L = NewLabel;
                  Emit(goto L);
                  }
                }
<block>
ENDIF          {  PostLabel(L) }
