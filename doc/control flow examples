aibece: (expected: abc)
    a
    if (true)
        b
    endif
    c 
    END
    
aibicedefe: (expected: abcdf)
    a
    if (true)
        b
        if (true)
            c
        endif
        d
    endif
    f
    END
    
    
aiblcede: (expected: abd)
    a
    if (true)
        b
    else
        c
    endif
    d
    END
    
aiiblcedifelgehjiiikeeee (expected: abdfhjk)
    a
    if (true)
        if (true)
            b
        else
            c
        endif
        d
        if (true)
            f
        endif
    else
        g
    endif
    h
    j
    if (true)
        if (true)
            if (true)
                k
            endif
        endif
    endif
    END
        
        
awbee (expected: abbbbb...)
    a
    while (true)
        b
    endwhile
    END
    
aiblwcedee (expected: ab)
    a
    if (true)
        b
    else
        while (true)
            c
        endwhile
        d
    endif
    END
    
arbue (expected: ab)
    a
    repeat
        b
    until (true)
    END
    
arbiceue (expected: abc)
    a
    repeat
        b
        if (true)
            c
        endif
    until (true)
    END
    
arbrcuue (expected: abc)
    a
    repeat
        b
        repeat
            c
        until (true)
    until (true)
    END