aibece: (expected: abc)
    a
    if (true)
        b
    endif
    c 
    END
    
aibicegehe: (expected: abcgh)
    a
    if (true)
        b
        if (true)
            c
        endif
        g
    endif
    h
    END
    
    
aiblcege: (expected: abg)
    a
    if (true)
        b
    else
        c
    endif
    g
    END
    
aiiblceminelgehjiiikeeee (expected: abmnhjk)
    a
    if (true)
        if (true)
            b
        else
            c
        endif
        m
        if (true)
            n
        endif
    else
        g
    endif
    h
    j
    if (true)
        if (true)
            if (true)
                k
            endif
        endif
    endif
    END
        
        
awbee (expected: abbbbb...)
    a
    while (true)
        b
    endwhile
    END
    
aiblwcegee (expected: ab)
    a
    if (true)
        b
    else
        while (true)
            c
        endwhile
        g
    endif
    END
    
arbue (expected: ab)
    a
    repeat
        b
    until (true)
    END
    
arbiceue (expected: abc)
    a
    repeat
        b
        if (true)
            c
        endif
    until (true)
    END
    
arbrcuue (expected: abc)
    a
    repeat
        b
        repeat
            c
        until (true)
    until (true)
    END
    
afc=bege (expected: abg, c = 1)
    a
    for c = (0) to (0)
        b
    endfor
    g
    END
    
adbece (expected: ac)
    a
    do (0)
        b
    enddo
    c
    END