/* 
    Driver program; runs compiler in an interactive mode.

*/

#include <iostream>
#include "compiler.hh"

int main () {
    

    ds_compiler::Compiler my_compiler();
    
    
    
    //while is != end of line (?)
    //read in from is to a stringstream
    //then call compile_intermediate(stringstream, std::cout)
    //see http://www.cplusplus.com/reference/sstream/stringstream/stringstream/
    
    
    return 0;
}

