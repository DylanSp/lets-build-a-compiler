/* 
    Driver program; runs compiler in an interactive mode.

*/


#include "compiler.hh"

int main () {
    
    ds_compiler::Compiler my_compiler(&std::cin, &std::cout);
    
    
    
    return 0;
}

